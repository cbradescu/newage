{% extends 'OroUIBundle:actions:view.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% import 'OroEntityConfigBundle::macros.html.twig' as entityConfig %}

{% block stats %}
{% endblock %}

{% oro_title_set({params : {"%entity.name%": entity.title|default('N/A') }}) %}

{% block navButtons %}
    {% if resource_granted('EDIT', entity) %}
        {{ UI.editButton({
        'path' : path('cb_newage_client_update', { id: entity.id }),
        'entity_label': 'cb.newage.client.entity_label'|trans
        }) }}
    {% endif %}
    {% if resource_granted('DELETE', entity) %}
        {{ UI.deleteButton({
        'dataUrl': path('cb_newage_client_api_delete_client', {'id': entity.id}),
        'dataRedirect': path('cb_newage_client_index'),
        'aCss': 'no-hash remove-button',
        'id': 'btn-remove-user',
        'dataId': entity.id,
        'entity_label': 'cb.newage.client.entity_label'|trans
        }) }}
    {% endif %}
{% endblock navButtons %}

{% block pageHeader %}
    {% set breadcrumbs = {
    'entity': entity,
    'indexPath': path('cb_newage_client_index'),
    'indexLabel': 'cb.newage.client.entity_plural_label'|trans,
    'entityTitle': entity.title
    } %}

    {{ parent() }}
{% endblock pageHeader %}

{% block content_data %}
    {%- set clientInformation -%}
    <div class="row-fluid form-horizontal">
        <div class="responsive-block">
            {{ UI.renderProperty('cb.newage.client.title.label'|trans, entity.title) }}
            {{ UI.renderHtmlProperty('cb.newage.client.description.label'|trans, entity.description|escape|nl2br) }}
        </div>

        <div class="responsive-block">
            {{ entityConfig.renderDynamicFields(entity) }}
        </div>
    </div>
    {%- endset -%}

    {% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}
    {% set gridName = 'client-scheduler_event-grid' %}
    {% set params = {client_id: entity.id} %}

    {%- set gridSchedulerEvents -%}
        {% if gridName is defined %}
            {% if gridScope is defined %}
                {% set gridName = oro_datagrid_build_fullname(gridName, gridScope) %}
            {% endif %}
            {% if params is defined %}
                {{ dataGrid.renderGrid(gridName, params) }}
            {% else %}
                {{ dataGrid.renderGrid(gridName) }}
            {% endif %}
        {% endif %}
    {%- endset -%}

    {% set dataBlocks = [
        {
            'title': 'cb.newage.client.block.general'|trans,
            'class': 'active',
            'subblocks': [
                {'data' : [clientInformation]}
            ]
        },
        {
            'title': 'cb.newage.panel.block.panel_views'|trans,
            'class': 'active',
            'subblocks': [
                {'data': [gridSchedulerEvents]}
            ]
        }
    ] %}


    {% set id = 'clientView' %}
    {% set data = { 'dataBlocks': dataBlocks } %}

    {{ parent() }}
{% endblock content_data %}
