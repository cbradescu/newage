{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set gridName = 'available-grid' %}
{% set params = {month: month} %}
{% set pageTitle = 'cb.newage.available.monthly'|trans %}

{% block navButtons %}
    {{ UI.clientButton({
        'dataUrl': path('cb_newage_campaign_create'),
        'aCss': 'no-hash',
        'iCss': 'icon-plus-sign',
        'title' : 'cb.newage.campaign.add.label'|trans,
        'label' : 'cb.newage.campaign.add.label'|trans,
        'widget' : {
            'type' : 'dialog',
            'multiple' : false,
            'options' : {
            'stateEnabled': false,
            'dialogOptions' : {
            'title' : 'cb.newage.campaign.add.label'|trans,
            'modal' : true,
            'allowMaximize': false,
            'allowMinimize': false,
            'resizable': false,
            'width': 700
        }
    }
    }
    })
    }}
{% endblock navButtons %}